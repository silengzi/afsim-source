# ****************************************************************************
# CUI
#
# The Advanced Framework for Simulation, Integration, and Modeling (AFSIM)
#
# The use, dissemination or disclosure of data in this file is subject to
# limitation or restriction. See accompanying README and LICENSE for details.
# ****************************************************************************

include_once processors/quantum_agents/aifl/fl_quantum_tasker_simple.txt

include_once sensors/radar/ew_radar.txt

platform_type FLIGHT_LEAD WSF_PLATFORM

  #side  blue / red

   comm sub_net RED_DATALINK
      network_name <local:master>
      internal_link data_mgr
      internal_link task_mgr
      internal_link perception
   end_comm

   comm cmdr_net RED_DATALINK
      network_name <local:slave>
      internal_link data_mgr
      internal_link task_mgr
      internal_link perception
   end_comm

   processor data_mgr WSF_TRACK_PROCESSOR
      #debug
      purge_interval             60    seconds
      report_to commander        via   cmdr_net
      report_to subordinates     via   sub_net to cmdr_net
      report_interval            1 sec
      fused_track_reporting      on
      raw_track_reporting        off
      circular_report_rejection  true
   end_processor

   processor perception WSF_PERCEPTION_PROCESSOR
      on
      #update_interval 10 sec
      script_debug_writes      on
      reporting_self           true
      reporting_others         true
      report_interval          10  sec
      report_to commander      via cmdr_net
      asset_perception         status_messages
      asset_update_interval     5 sec
      max_asset_load           10
      threat_update_interval    5 sec
      max_threat_load          10
      heat_map
       position 16:18:00.00s 50:41:00.00e altitude 30000.00 ft
       grid_extent 200 km
       cell_size 5 km
       sensor_range 60 nm
       heading_change_limit 5 deg
       use_asset_perception on # Use subordinate asset radar to "clear"
       heat_decay 0.2
       decay_interval 30 sec
       expansion_timeout 5 min
       # Commands that control drawing
       # activate to see representation of the grid
       draw_grid on
       draw_heat on
       draw_sensor_outline on
     end_heat_map
   end_processor

   processor  task_mgr  FL_QUANTUM_TASKER
    #script_debug_writes on
    #show_task_messages
     script_variables
        mSelfCreateTasks = true;
     end_script_variables
   end_processor

   sensor ew_radar EW_RADAR
     off
     internal_link data_mgr
     ignore_same_side
   end_sensor

end_platform_type
