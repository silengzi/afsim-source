# ****************************************************************************
# CUI
#
# The Advanced Framework for Simulation, Integration, and Modeling (AFSIM)
#
# The use, dissemination or disclosure of data in this file is subject to
# limitation or restriction. See accompanying README and LICENSE for details.
# ****************************************************************************
//******************************************************************
// This file wraps functions in the WsfScriptSixDOF_MoverTestObject
// so that the user does not need to type the same block of code
// to get the mover from the platform.
//******************************************************************

// This uses maximum throttle (value=2.0, afterburner). If the vehicle
// does not have an afterburner, it will use full throttle
// (value=1.0, military power) instead.
script void FlyStraightLevelFullThrottle(WsfPlatform aPlatform)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().FlyStraightLevelFullThrottle();
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().FlyStraightLevelFullThrottle();
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script double GetRangeSinceStart_km(WsfPlatform aPlatform)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      return rigidBodyMover.__getTestObject().GetRangeSinceStart_km();
   }
   else if(pointMassMover.IsValid())
   {
      return pointMassMover.__getTestObject().GetRangeSinceStart_km();
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
      return false;
   }
end_script

script void SetMasterNoLagTesting(WsfPlatform aPlatform, bool aEnable)   
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterNoLagTesting(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterNoLagTesting(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterFreezeAltitude(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterFreezeAltitude(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterFreezeAltitude(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterFreezePitch(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterFreezePitch(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterFreezePitch(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterFreezeRoll(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterFreezeRoll(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterFreezeRoll(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterFreezeVerticalSpeed(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterFreezeVerticalSpeed(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterFreezeVerticalSpeed(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterFreezeYaw(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterFreezeYaw(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterFreezeYaw(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterFreezeFuelBurn(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterFreezeFuelBurn(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterFreezeFuelBurn(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script void SetMasterNoAlphaTesting(WsfPlatform aPlatform, bool aEnable)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().SetMasterNoAlphaTesting(aEnable);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().SetMasterNoAlphaTesting(aEnable);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script

script int CanFlyAt(WsfPlatform aPlatform, double aAltitude_ft, double aVelocity_fps, double aThrottle, double aFlaps)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      return rigidBodyMover.__getTestObject().CanFlyAt(Math.M_PER_FT() * aAltitude_ft, Math.M_PER_FT() * aVelocity_fps, aThrottle, aFlaps);
   }
   else if(pointMassMover.IsValid())
   {
      return pointMassMover.__getTestObject().CanFlyAt(Math.M_PER_FT() * aAltitude_ft, Math.M_PER_FT() * aVelocity_fps, aThrottle, aFlaps);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
      return -1;
   }
end_script

script void CreateAutopilotSupportFile(WsfPlatform aPlatform, string aFilename)
   WsfRigidBodySixDOF_Mover rigidBodyMover = (WsfRigidBodySixDOF_Mover)aPlatform.Mover();
   WsfPointMassSixDOF_Mover pointMassMover = (WsfPointMassSixDOF_Mover)aPlatform.Mover();
   if(rigidBodyMover.IsValid())
   {
      rigidBodyMover.__getTestObject().CreateAutopilotSupportFile(aFilename);
   }
   else if(pointMassMover.IsValid())
   {
      pointMassMover.__getTestObject().CreateAutopilotSupportFile(aFilename);
   }
   else
   {
      writeln("--ERROR-- Not a SixDOF Mover.");
   }
end_script
